### Install virtualenv 

`sudo apt-get install python3-venv`

create virtualenv

`sudo apt-get install python3-venv`

Activate virtualenv

`source bin/activate`

Dependencies

`pip install -r requirements.txt`

